export const themeDefaultConfigs = {
  unit: 8,
  border: 1,
  roundRadius: "1.5em",
  roundedRadius: "0.7em",
  squareRadius: "0em",
  timings: {
    faster: {
      duration: "50ms",
      easing: "ease-in-out",
    },
    fast: {
      duration: "100ms",
      easing: "ease-in-out",
    },
    normal: {
      duration: "200ms",
      easing: "ease-in-out",
    },
    slow: {
      duration: "400ms",
      easing: "ease-in-out",
    },
    slower: {
      duration: "600ms",
      easing: "ease-in-out",
    },
  },
};
